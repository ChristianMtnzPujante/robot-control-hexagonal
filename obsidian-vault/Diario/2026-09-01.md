---
tags: [diario]
---

# 2026-09-01

Rama de experimentación al margen del seguimiento habitual
(`experimento/planificador-evita-obstaculo`), fusionada a `main` el mismo
día. Detalle completo de la práctica en [[Evitación de Colisiones]] — aquí
solo el orden de los hechos.

- Primer planificador ([[ObstacleAvoidingPlanningAdapter]]): evitación
  mínima del tip. `PoeKinematicsAdapter.forward_kinematics()` expuesto como
  método público. Demo contra CoppeliaSim reveló el desajuste de marcos
  entre `cr5_base.ttt` y la home matemática de PoE. Commit `cc12cec`.
- Escena construida por código desde el URDF real
  (`coppeliasim_scene_builder.py`, plugin `simURDF`) — el desajuste de
  marcos desaparece por construcción. Hallazgo real: el robot importado
  quedaba en modo dinámico, no cinemático — forzado
  `jointmode_kinematic`/`modelproperty_not_dynamic` tras importar. Commit
  `0f913ad`.
- Segundo ejemplo (`joint2=90°`) + refactor `avoid_obstacle_demo.run(...)`.
  Commit `2214611`.
- Bloque 4 tarea 5: `PoeKinematicsAdapter.link_poses()` (pose de cada
  articulación) y [[WholeBodyObstacleAvoidingPlanningAdapter]] (evitación de
  cuerpo completo). Hallazgo serio: IK convergiendo a más de una vuelta
  (`joint4=-457°`) — `_within_a_full_turn` como salvaguarda. Commit
  `c900361`.
- Demo comparativo lado a lado (`avoid_obstacle_demo_compare.py`, puertos
  23000/23001). Commit `1c9e06b`.
- Decisión de diseño (DDD): PoE y CGA son *bounded contexts* separados, no
  una `Scene` compartida reinterpretada — ver
  [[Decisiones de Diseño Clave]]. Commits `02a1934`, `11d82e8`. Vikunja
  #84/#85/#86 enlazadas.
- Fusión completa a `main` (`a29d8aa`), build + 36 tests en verde, subido al
  remoto.
- Sesión de revisión de código línea a línea trasladada a comentarios
  paso a paso en el propio código (no solo el "por qué") — commits
  `6fd883d`, `b120a4e`, `55c3110`, `47a1d27`. Guardado como preferencia
  general del usuario (memoria).
- Incidencia de sesión: uso indebido de una herramienta de espera en
  segundo plano para las simulaciones largas de CoppeliaSim — corregido en
  cuanto se señaló.

## Ver también

- [[Evitación de Colisiones]]
- [[ObstacleAvoidingPlanningAdapter]]
- [[WholeBodyObstacleAvoidingPlanningAdapter]]
- [[Decisiones de Diseño Clave]]
